<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.bo.projectrocket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/image_volante"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/image_volante"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".ImageAnswerActivity"></activity>
        <activity android:name=".TextQuestionActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".ImageQuestionActivity" />
        <activity android:name=".WinActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".InitActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>